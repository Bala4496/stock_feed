version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto17

  pre_build:
    commands:
      - echo "Install Maven"
      - yum install -y maven
      - mvn --version

  build:
    commands:
      - echo "Compile and run tests"
      - mvn clean test
